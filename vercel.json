{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "env": {
    "DATABASE_URL": "postgresql://<username>:<password>@<host>:<port>/<database>"
  },
  "buildCommand": "npm run generate && npm run migrate && npm run seed && npm run build"
}
